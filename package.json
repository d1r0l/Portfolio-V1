{
  "name": "homepage",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev-host": "vite --host",
    "build": "rm -rf dist/* && cp index.html dist/ && cp style.css dist/ && cp -r assets dist/ && cp -r js dist/ && fly deploy"
  },
  "devDependencies": {
    "vite": "^5.1.4"
  }
}
